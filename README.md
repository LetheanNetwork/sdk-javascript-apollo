# @lthn/sdk-javascript-apollo

Apollo GraphQL client (deprecated by upstream — name carries the -deprecated suffix in 7.x).

Auto-generated from the lthn OpenAPI spec at:

  https://github.com/LetheanNetwork/desktop/blob/main/build/sdk/openapi.yaml

Do **not** hand-edit. Regenerate via:

  ```bash
  cd /path/to/desktop
  ./build/sdk/publish.sh javascript-apollo
  ```

## Install

```bash
npm install @lthn/sdk-javascript-apollo
```

## License

EUPL-1.2 — same as the upstream lthn project.
